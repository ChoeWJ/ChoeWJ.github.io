<nav class="nav__list">
  {% for category in site.data.navigation.docs %}
    <span class="nav__sub-title" style="font-size: 18px; font-weight: bold;">{{ category.title }}</span> <!-- 메인 카테고리 -->
    <ul class="nav__items">
      {% for item in category.children %}
        {% if item.subcategories %}
          <li>
            <strong>{{ item.title }}</strong> <!-- 서브 카테고리 -->
            <ul>
              {% for subitem in item.subcategories %}
                <li>
                  <a href="{{ subitem.url }}" style="font-size: 14px;">{{ subitem.title }}</a>
                </li>
              {% endfor %}
            </ul>
          </li>
        {% else %}
          <li>
            <a href="{{ item.url }}" style="font-size: 12px;">{{ item.title }}</a>
          </li>
        {% endif %}
      {% endfor %}
    </ul>
  {% endfor %}
</nav>